import CharityBlogSection from '../../../lib/organisms/components/portfolios/portfolio4';

const resolveComponents = () => {
  return {
    403389: {
      render: props => <CharityBlogSection {...props} />,
      name: 'Blog Section Data Section',
      category: 'Section',
    },
  };
};
export default resolveComponents;
