export type NavbarDataType = {
    logoSrc?: string; // The logo img src
    logoTitle?: string; // The logo img title
    whiteLogoSrc?: string; // The white version of the logo img src
    ctaLink?: string; // call to action link, examples (letsTalkLink, button_url)
    ctaLabel?: string; // call to action label, examples (letsTalkLink, button_url)
    backgroundImage?: string;
    jumpToSectionLink?: string; // examples (anchorLink)
    jumpToSectionLabel?: string; // examples (anchorLinkLabel)
}